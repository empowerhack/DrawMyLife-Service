$('#users .container table').append("<%= escape_javascript(render 'users') %>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@users, 'MORE', remote: true, id: 'load_more')) %>");
if (!$('#load_more').length) { $('#paginator').remove(); }
